node_modules
.env
.pnpm-debug.log
dist
.DS_Store
.vscode/settings.json

#Dotfiles 
.dotfiles/

# Build output: a pure function of the tree, so committing it would be a second source of truth.
worker/src/generated/
dist-static/
dist-github/
dist-meta/
.dry/

# wrangler scratch (bundles, local R2/KV state). Gitignored so biome skips it too.
.wrangler/

# Local credential files for tools/setup-cloudflare.sh. Never commit these.
.cf-token
.cf-worker-token
.r2-access-key-id
.r2-secret-access-key
